% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cptAR.R
\name{cptAR}
\alias{cptAR}
\title{Autoregressive Changepoint Detection}
\usage{
cptAR(
  data,
  order = 1,
  trend = FALSE,
  method = "PELT",
  penalty = "MBIC",
  minseglen = 3
)
}
\arguments{
\item{data}{A numeric vector containing the time series data.}

\item{order}{Integer. The order of the AR model (1 or 2). Default is 1.}

\item{trend}{Logical. Should a linear trend be included? Default is FALSE.}

\item{method}{Character. The changepoint algorithm to use ("PELT" or "AMOC"). Default is "PELT".}

\item{penalty}{Character. The penalty to use (e.g., "MBIC", "AIC"). Default is "MBIC".}

\item{minseglen}{Integer. Minimum segment length. Default is 3.}
}
\value{
An object of class \code{cpt.reg} from the \code{changepoint} package.
}
\description{
Fits a changepoint model to time series data using an Autoregressive (AR1 or AR2)
structure, optionally with a linear trend.
}
\examples{
set.seed(42)
# Simulate AR(1) with a change in phi
x <- c(arima.sim(list(ar = 0.8), n = 100), arima.sim(list(ar = -0.5), n = 100))

# Fit the model
result <- cptAR(x, order = 1, trend = FALSE)
changepoint::cpts(result)
}
